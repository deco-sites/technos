import Component from "$store/components/footer/NewsletterModal.tsx";
import type { Props } from "$store/components/footer/NewsletterModal.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
